import React, { useState } from "react";

const ToggleVisibility = ({ children }) => {
  const [show, setShow] = useState();

  return <div>ToggleVisibility</div>;
};

export default ToggleVisibility;
